import React from 'react'
import ApproverList from './approverList'
import { Box } from '@mui/material';

const ApproverTable = ({data}) => {
console.log(data)
    return (
    <Box>Approver table</Box>
  )
}

export default ApproverTable